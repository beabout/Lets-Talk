%h1 Topics
%table.table
  - @topics.each do |t|
    %tr
      %td= t.title
      %td= t.type
      %td= link_to 'conversations', topic_path(t), class: 'btn small-btn'

- if current_user
  = link_to 'create topic', new_topic_path