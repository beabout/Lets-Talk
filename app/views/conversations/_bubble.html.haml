.row
  - if orient
    .bubble.left
      = message.text
      %br
      %note= message.time_and_sender
  - else
    .bubble.right.alt-bg
      = message.text
      %br
      %note= message.time_and_sender